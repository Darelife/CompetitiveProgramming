#include <bits/stdc++.h>
using namespace std;


int main() {
  int n;
  cin >> n;
  vector<int> a;
  // a.push_back(1);
  for (int i = 0; i < n; i++) {
    a.push_back(i);
  }
  // cout << sizeof(a) / sizeof(a[0]) << endl;
  cout << a.capacity() << endl;
  cout << a.size() << endl;
  set<int> s;
  s.
  return 0;
}
